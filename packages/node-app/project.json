{
    "name": "node-app",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/node-app/src",
    "projectType": "application",
    "targets": {
        "serve": {
            "executor": "@nrwl/js:node",
            "options": {
                "buildTarget": "node-app:build"
            },
            "configurations": {
                "production": {
                    "buildTarget": "node-app:build:production"
                }
            }
        },
        "type": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npx tsc -b packages/node-app/tsconfig.json"
            }
        },
        "lint": {
            "executor": "@nrwl/linter:eslint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "fix": true,
                "lintFilePatterns": ["**/*.{ts,tsx,js,jsx}"]
            }
        },
        "test": {
            "executor": "@nrwl/vite:test",
            "outputs": ["{projectRoot}/coverage"],
            "options": {
                "reportsDirectory": "../../coverage/node-app"
            }
        }
    },
    "tags": []
}
